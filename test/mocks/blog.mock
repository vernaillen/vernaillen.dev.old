import { vi } from 'vitest'

const getPosts = vi.fn()
const getImageUrl = vi.fn()
const getRoutes = vi.fn()

export const blog = vi.mock('blog', () => ({
  getPosts,
  getImageUrl,
}))

export const vueRouter = vi.mock('vue-router', () => ({
  useRouter: () => ({
    getRoutes,
  }),
}))
